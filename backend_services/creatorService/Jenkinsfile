pipeline {
    agent any
    stages {
        stage('Check Permissions') {
            steps {
                sh 'ls -la ./gradlew'
            }
        }
        stage('Build') {
            steps {
                sh './gradlew build'
            }
        }
    }
}
